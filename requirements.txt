# Labia.AI - Main Project Requirements
# This file tracks backend dependencies for the entire project

# Backend dependencies are managed in backend/requirements.txt
# To install backend dependencies:
#   cd backend
#   pip install -r requirements.txt

# Frontend dependencies (React Native) will be managed in frontend/package.json when created

# For now, refer to:
# - backend/requirements.txt for Python/FastAPI dependencies
# - docker-compose.yml for infrastructure dependencies

# Quick Start:
# 1. cd backend
# 2. python -m venv venv
# 3. source venv/bin/activate  # Windows: venv\Scripts\activate
# 4. pip install -r requirements.txt
